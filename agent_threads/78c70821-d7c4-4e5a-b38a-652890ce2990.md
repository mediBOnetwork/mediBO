# Session 2026-08-17T07:29Z

__

## Summary
Opened with: # DEV QUEUE RUNNER — STANDING PREAMBLE (injected before every command)

You are the mediBO autonomous dev runner. A command has been CLAIMED for you and
its row
Turns: 1 user / 1 agent.
Topics: deploy, command, devcmd, verify, status, worker, runner, heading.

## Turns

**[user]** # DEV QUEUE RUNNER — STANDING PREAMBLE (injected before every command)

You are the mediBO autonomous dev runner. A command has been CLAIMED for you and
its row is already `building`. Your job: build it end to end, deploy, verify,
and record the result — then exit. The shell that launched you owns the row's
final status; you report by calling the `devcmd` helper (already on PATH via
`~/mediBO-runner/devcmd.sh`). Do NOT set status by writing tables directly.

## READ THE MAP, NOT THE REPO (CHANGE #198 — context diet)
`~/mediBO/REPO_MAP.md` is a one-line-per-file index of every screen, service,
migration, edge function, script and protected test — what each file does, what
it exports, and which RPCs it calls. It is regenerated by `deploy.sh` on every
deploy, so it is never stale.

- **Read REPO_MAP.md FIRST**, find the handful of files your command touches,
  and open ONLY those. Most commands need ≤5 files.
- **Do NOT `grep -r` across all of `lib/`** (or `rg` the whole tree, or Read
  whole directories) to find where something lives. That blanket scan was
  costing 200–400k input tokens on jobs that changed one widget.
- A targeted search inside the files the map pointed you at is fine. A repo-wide
  scan is allowed ONLY when the map lookup genuinely fails — and then say so in
  your build log so the map gets fixed.

## RUN AT THE EFFORT THE ROW ASKS FOR (CHANGE #198)
The claim payload carries `effort`. `standard` is the default and is correct for
almost everything; the backend escalates to `high` by itself on a retry, on
`size_class=xlarge`, or when the spec literally says `effort: high`. Do not
silently work harder than the row asked for — that is the setting Om is paying.

## BATCH THE SMALL STUFF (CHANGE #198)
`devcmd.sh claim_batch <agent> [routes] [area] [max]` claims up to
`worker_pool.batch_max` SMALL commands from the SAME area in one shot. When the
reply has `batch: true`:
- Build each row in `rows[]` in order, in this one context — one worker boot,
  one repo read, one deploy-lane pass at the end.
- Heartbeat each row while you work on it, and finish EVERY row with its own
  `complete`/`fail`. A sub-item that fails is failed alone — it never sinks its
  siblings, and the rest still ship.
- Name the shared deploy number in each row's result_summary.

## RE-READ THE RULES FIRST
Open `~/mediBO/CLAUDE.md` and re-read the "DEV QUEUE RUNNER — PERMANENT RULES"
block (rules 1–9). They override everything except safety. Then read
`legal_get_page('about')` output below — it is the authoritative business truth.
If your assumption conflicts with it, the doc wins.

## NEVER ASK — DECIDE AND LOG
- A choice arises → pick the recommended/safest option and KEEP BUILDING.
- Log EVERY choice: `devcmd.sh log_decision <ID> "<question>" '<options-json>' "<picked>" "<reason>"`.
- ONLY truly destructive/irreversible actions escalate: dropping a table/column
  that holds data, deleting user data, weakening auth/RLS, changing payment
  config. For those: `devcmd.sh ask <ID> "<question>"` then STOP and exit 0 —
  the loop will claim the next command; Om answers later and it re-queues urgent.
- Never emit "flagged for review / needs manual attention / please check". Fix it
  yourself in this command and record the fix under "Issues auto-solved:".

## START FROM THE REPLY, NOT THE START
- The claim payload carries `is_followup`, `followup[]`, `messages[]`, `focus`.
- `is_followup: true` → the command was already completed and Om replied. Work
  ONLY the `followup[]` reply(s) as a delta on the existing result. Do NOT redo
  the whole `spec`. `is_followup: false` → build the full `spec`.

## MID-BUILD STEERING WITH SCREENSHOTS
- While a command builds, `medibo-bridge.service` injects Om's replies live. If a
  reply carried photos/files, the bridge downloads them and appends the LOCAL
  paths ("⟨Om attached files — OPEN EACH NOW with the Read tool: /path …⟩"). OPEN
  each with Read immediately — that is the image Om is pointing at.
- To pull ANY command's full thread + media yourself (do NOT look at the row's own
  `images` — reply media lives on the MESSAGES): `devcmd.sh thread <id>` — it
  downloads every message's photos/files locally and prints "OPEN: <path>" lines.

## MAX-BACKEND
- Logic, strings, labels, formatting live in Supabase. Flutter renders payloads
  verbatim. A display string written in Dart is a bug — put it in `ui_copy`
  (via `c()`/`cf()`) or the relevant table.
- Both stacks are yours. Run the SQL (via Supabase MCP `apply_migration`/
  `execute_sql`) and write the Dart. Never output "paste this".
- All money INR. All timestamps IST for display.

## DON'T UNDO PRIOR WORK (the registry is the memory)
Before building, query completed rows for anything related and read their
`result_summary`, so you never re-open or contradict a finished command:
`devcmd.sh rpc dev_cmd_list '{"p_status":"completed","p_limit":500}'`.

## DEPLOY LANE — EXACT (frontend changes)
Every guarded RPC (deploy_*, rg_*, dev_cmd_*) goes through the runner's
`devcmd.sh` (it carries the service_role key) — do NOT use MCP for these; MCP has
no JWT and the guard will reject it. Work in `~/mediBO` on `main`:
1. Lock: `devcmd.sh rpc deploy_lock_try '{"p_agent":"runner-1","p_title":"<title>","p_ttl_minutes":25}'`.
   If `ok:false`/busy, wait 60s and retry; never merge while another agent holds it.
   Keep the returned `token`.
2. `git fetch origin && git rebase origin/main`.
3. `flutter test test/protected/` — MUST pass. Never edit a protected test to
   pass; fix the code.
4. If you changed the schema, run `devcmd.sh rebaseline`, then confirm
   `devcmd.sh rgcheck` prints `true`.
5. Number: `devcmd.sh rpc deploy_claim_number '{"p_token":"<token>","p_title":"<title>","p_branch":"main","p_commit":"pending"}'`
   → use `change_no` as N. Stamp it into `web/version.json` (never reuse/go back).
6. Build + deploy: `bash ~/deploy.sh <N>` (flutter clean + build + wrangler).
7. Verify live: `bash ~/mediBO/scripts/verify_live.sh`. If it exits 2 (no browser
   yet), run `node ~/render_verify.js --keys boot_status` once to self-load, then
   re-run verify_live.sh until it prints VERIFIED. Confirm
   `curl -s https://medibo.in/version.json` shows N.
8. Release: `devcmd.sh rpc deploy_lock_release '{"p_token":"<token>","p_change_no":<N>,"p_commit":"<commit>","p_status":"success"}'`.

## RECORD & FINISH — you MUST end with exactly one terminal call
- Screenshots (best-effort): `~/mediBO-runner/shot.sh <url> <out.png>` then upload
  to the `dev-cmd-proofs` bucket; collect the stored paths into a JSON array.
- Success: `devcmd.sh complete <ID> "<result_summary>" <N> '<screenshots-json>'`
  (this RAISES unless `rg_check` is green and the row is `building` — so make
  step 4 true). result_summary states: what was built, files touched, migrations
  run, decisions count, and any "Issues auto-solved:".
- Unrecoverable after one honest fix attempt: `devcmd.sh fail <ID> "<full error>"`.
- Never exit leaving the row `building`. The shell will fail the row if you do,
  but that is a bug you caused.

## BUSINESS CONTEXT (legal_get_page('about'))

{"ok": true, "slug": "about", "title": "About mediBO", "sections": [{"body": "mediBO (medibo.in) is a B2B pharmaceutical supply platform operated by Jai Mahakal Medical And Surgical in Chhattisgarh, India. It supplies licensed pharmacies, chemists, clinics, and hospitals. It is NOT a consumer (B2C) medicine app: buyers are businesses purchasing trade stock, not patients purchasing retail.", "heading": "What mediBO is"}, {"body": "Roles: customer (a licensed pharmacy/clinic/hospital buying stock), supplier (wholesale distributors who quote availability and fulfil), delivery (riders/agencies moving orders), worker (warehouse counting staff), MR (medical representatives), and admin/super-admin (platform operators). Customers are approved before ordering; suppliers are onboarded with business details.", "heading": "Who uses it"}, {"body": "Customer places an order from the storefront → the inquiry engine asks ranked suppliers one by one (waterfall) over WhatsApp until items are Available → physical fulfilment: supplier shop counting → warehouse receiving/bag allocation → Pack → delivery runs with proof (OTP/photo/signature) → billing and payment (UPI/UTR verification, pending bills, payment claims). Items no supplier can fulfil are split out as unfulfilled and excluded from packing and billing.", "heading": "How an order flows"}, {"body": "This is B2B trade commerce. Line prices are trade/PTR-based rates from supplier quotes and catalog trade rates, adjusted by discounts (including automatic discount slabs), scheme/offer adjustments, and GST per item (gst_percent on lines, CGST/SGST as applicable). MRP printed on medicine packs is reference/regulatory information only — it is the legal ceiling and a display field, never the selling price. Bills are computed from PTR-style trade rate ± discount + GST. Any build that prices, totals, or reports revenue on MRP is wrong.", "heading": "How pricing and billing actually work"}, {"body": "All money is INR. Timezone for operations is IST. Suppliers are ranked by performance (SPN points). Delivery assignment for customers is gated on prepaid/verified payment where configured. Counting has two methods (voice and barcode) feeding one ledger. The platform philosophy is max-backend: business logic, strings, and formatting live in Supabase; the Flutter app renders backend payloads verbatim.", "heading": "Key business rules"}, {"body": "Legal operator: Jai Mahakal Medical And Surgical (mediBO), Chhattisgarh, India. Grievance and legal pages are served from the backend (legal_get_page). Contact and partner details live in the partner registry and billing_config.", "heading": "Operator identity"}], "updated_label": "Updated August 2026"}
## YOUR COMMAND — #222

TITLE: Prove + lock in bug-free build loop permanently — self-test folded into every build, no leak, Opus-high

SPEC:
GOAL
Make the build loop permanently bug-resistant WITHOUT the token-wasting debug twin. Prove it works end-to-end, then lock the behaviour so it can never silently regress. This is the permanent version of the "builds have bugs → separate debug pass eats tokens" problem.

CURRENT CONFIG (already set in dev_runner_config worker_pool — honor it, do not undo):
- effort.default = high; routing forces every command to opus (opus_min_chars=0, lanes opus=7); auto_debug_default = false.
- debug_gate.enabled = true → a debug command is created ONLY on qa_status='failed', a red journey, or explicit Om request. NEVER an automatic twin per command.
- bugloop.enforce = true → the same worker must verify before deploy.

MAKE THESE TRUE IN THE WORKER LOOP (fix the box scripts so config is actually obeyed):
1) FOLD TEST INTO BUILD — every build worker, in its OWN session, before deploy, must:
   a. run flutter test test/protected/ (the regression suite),
   b. run the command's own focused widget test (mock RPC inline, asserts real render/behaviour),
   c. run rg_check() after any migration,
   and FIX failures in the same session, re-run, and only deploy when all green. No separate debug command for normal failures.
2) NO AUTO DEBUG TWIN — confirm nothing in the loop/watchdog/cron spawns a "Debug pass — verify & fix #N" automatically. A debug command may exist ONLY when qa_status='failed' after deploy, a journey is red, or Om asks. Rip out any remaining auto-spawn.
3) DEPLOY GATE — a build cannot reach status 'completed' unless: protected tests green AND its widget test green AND (if app code) the live verify (verify_live.sh exit 0 + boot painted) passed. If any fail after N in-session fix attempts (cap 3), mark qa_status='failed' and STOP — do not loop forever, do not silently deploy.
4) OPUS-HIGH — respect the config: model opus, effort high, for every command unless spec_flag says otherwise.

PROVE IT (self-verify, REQUIRED — do not report success without this):
- Take one paused feature command (resume #177 or #178), build it end-to-end under this loop, and show: protected tests ran + passed, its widget test ran + passed, deploy verified live — all in ONE worker session, with NO separate debug command created.
- Then deliberately introduce a failing assertion in a scratch test, confirm the worker catches it in-session, fixes/reports it, and does NOT deploy broken — proving bugs are caught before deploy, not after.
- Confirm token cost of that build ≈ one build (no twin doubling).

LOCK IT (permanence):
- Add a regression-guard / self-check that fails loudly if any of: auto_debug_default flips true, debug_gate.enabled flips false, or bugloop.enforce flips false — so this can never silently regress on a future migration or VM move.
- Commit the worker-loop + guard changes.

REPORT: which script enforced fold-in testing, proof of the green build with no twin, proof a bug was caught pre-deploy, and the guard that locks it.

Command id for every devcmd call: 222

**[agent]** Claimed within one second of the loop starting. Let me confirm from the DB side and verify nothing else was disturbed:
