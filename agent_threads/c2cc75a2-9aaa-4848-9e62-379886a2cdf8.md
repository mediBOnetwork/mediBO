# Session 2026-08-17T06:57Z

__

## Summary
Opened with: Fix the worker-launch bug first, then do the permanent build-loop fix.

IMMEDIATE BUG: the supervisor launches each worker's claude into the interactive "Rewind
Turns: 1 user / 0 agent.
Topics: worker, runner, command, deploy, claude, protected, resume, session.

## Turns

**[user]** Fix the worker-launch bug first, then do the permanent build-loop fix.

IMMEDIATE BUG: the supervisor launches each worker's claude into the interactive "Rewind / Nothing to rewind to yet" resume screen instead of the headless runner loop, so no worker ever claims a command (#222 and others stay pending forever). Also there's a broken env line "Invalid environment variable name … s#claude-" and the orphan-cleanup drop-in's building-row query FAILED. Find the launch command in the runner scripts (~/mediBO-runner/*.sh — supervisor.sh / runner_loop.sh) and fix it so each worker starts claude non-interactively straight into the claim→build loop (skip the resume/rewind UI, pass the runner prompt/flags correctly, fix the s#claude- env var). Verify: a freshly spawned worker claims a pending command within one tick.

THEN make the build loop permanently bug-resistant (honor dev_runner_config worker_pool, do not undo it):
- effort high, opus for every command, auto_debug_default=false, debug_gate only on qa_status='failed'/red journey/explicit request, bugloop.enforce=true.
- FOLD TEST INTO BUILD: each worker, before deploy, runs flutter test test/protected/, the command's own focused widget test, and rg_check() after any migration; fixes failures in the same session; deploys only when all green. No separate auto debug twin.
- DEPLOY GATE: cannot reach 'completed' unless protected tests green AND widget test green AND (if app code) verify_live.sh passed. Cap in-session fix attempts at 3, then mark qa_status='failed' and stop — never silently deploy, never loop forever.
- LOCK IT: add a regression-guard that fails loudly if auto_debug_default flips true, debug_gate.enabled flips false, or bugloop.enforce flips false.

PROVE IT: resume #177 or #178, build end-to-end in one session showing protected+widget tests passed and deploy verified live with NO separate debug command; then introduce a failing assertion in a scratch test and confirm the worker catches it pre-deploy and does not deploy broken. Commit the runner-loop + guard changes. Report which script had the bad launch line, the exact fix, and proof of a green build with no twin.
