# Session 2026-08-16T15:14Z

__

## Summary
Opened with: what are the permanet rule saved in claude.md give the full list
Turns: 3 user / 2 agent.
Topics: backend, frontend, verify, feature, rontend, render, account, ermanent.

## Turns

**[user]** what are the permanet rule saved in claude.md give the full list

**[user]** i neef omplete list

**[agent]** Om's Eight Permanent Rules (from CLAUDE.md, marked "highest priority — never drop"):

**1. Answer in 10 lines max.** Title + description, 5 words/line, 50 words total. No preamble, no closing offer.

**2. Never flag — fix.** A problem you can fix isn't a finding, it's unfinished work. Report only after fixing.

**3. Decide, don't ask.** Pick your recommended option, execute, state it in one line. Ask only for irreversible data loss.

**4. Migration must not change behaviour.** Frontend↔backend moves must leave the app identical. "Mostly works" = failure.

**5. On a clash, backend wins.** Same logic both sides? Delete the frontend copy, never the backend.

**6. Maximum backend.** Everything computed, decided, formatted, worded in backend. Frontend only renders and sends input.

**7. But every feature needs a frontend path.** Backend-only capability = not delivered. Add the surface Om can use.

**8. Fast on slow/no internet.** Cache as render fallback, never authority. Queue writes, await, render. No optimistic state.

**[user]** give me starting to end full list in single response

**[agent]** The complete set of standing rules in CLAUDE.md:

**Top rule — THE APP RENDERS, IT NEVER DECIDES.** If a value could come from the backend, it MUST come from the backend. Frontend only: call RPC, render, send input.

**§1 Authority — execute, don't ask.** Use your DB/Supabase access. Stop only for irreversible data loss (DROP TABLE/COLUMN, TRUNCATE, DELETE of rows you didn't create).

**§2 Four questions the app must never answer:** Who am I? (`my_session()`) · What may I do? (backend boolean) · What do I show? (backend strings) · What is the data? (one RPC/screen).

**§3 Forbidden in frontend:** role branches, route literals, Dart strings, computing totals, sorting/filtering, formatting, deriving state, caching as truth, optimistic updates, direct table writes, `auth.currentUser` as account.

**§4 Required patterns:** one RPC/screen · key to account not login · never null in payload (but preserve semantics) · server-owned state · config as data.

**§5 How to work a bug:** instrument first · verify with data · one change at a time · test destructively/roll back · sweep don't patch · fix at source · say when impossible.

**§6 Per case:** new feature (backend first) · bug fix (find wrong side) · update (data not deploy) · refactor (backend direction only) · schema (account id day one).

**§7 Before done:** every claim executed · no null payloads · no Dart decisions · structural fix · real build+deploy+verify.

**Session Hygiene:** autoCompact ON, never manual /compact; /clear loses nothing.

**Verification Rule:** run `verify_live.sh`; never Puppeteer/CDP on Flutter canvas; Om does visual checks only.

**Cost Safety:** never call Google Matrix APIs; use OSRM.

**Defensive Import Rule:** no dart:html/dart:js outside main.dart.

**Boot Resilience Rule:** keep runZonedGuarded + per-step try/catch + 5s timeout.

**View As:** `kEnableViewAs=false` before public launch; it's write-capable.

**Supabase MCP:** PAT in settings.json; regenerate if auth fails; never ask Om to paste SQL.

**Om's Eight Permanent Rules:** (1) 10 lines max (2) never flag—fix (3) decide don't ask (4) migration keeps behaviour (5) clash → backend wins (6) maximum backend (7) every feature needs frontend path (8) fast on slow/no internet.
