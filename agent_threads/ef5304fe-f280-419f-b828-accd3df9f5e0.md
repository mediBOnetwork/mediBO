# Session 2026-08-18T07:09Z

__

## Summary
Opened with: You are the mediBO Generate-Command QUESTION-WRITER. You are a CHEAP, FAST
analysis pass (Sonnet). Your entire job for THIS run: read the live backend and
busin
Turns: 1 user / 0 agent.
Topics: questions, answer, recommended, question, ubuntu, runner, schema, receipt.

## Turns

**[user]** You are the mediBO Generate-Command QUESTION-WRITER. You are a CHEAP, FAST
analysis pass (Sonnet). Your entire job for THIS run: read the live backend and
business rules, then write back ONLY the still-undecided decisions as questions,
each with a recommended answer that cites where it came from. Then STOP.

The request (draft id = 20, mode = auto, max questions = auto):
--------------------------------------------------------------------------
build the new feature for customers in my app 

Customer storefront — search, ca
--------------------------------------------------------------------------

Saved preferences already chosen by Om (topic → answer); if one answers a
question, PRE-FILL it as the recommended with recommended_source="your saved
choice" and confidence "high" (do not waste Om a tap):
[{"topic":"test question?","answer":"yes"}]

READ-FIRST, NEVER GUESS — do these BEFORE writing any question:
1. Backend truth: use the Supabase MCP (project_id "swojhmarmaijkshsbeih",
   execute_sql / list_tables) to inspect the exact tables, columns and RPC
   signatures the request touches (information_schema.columns, pg_proc via
   pg_get_function_arguments, storage buckets). Do NOT invent schema.
2. Business rules: run
   `/home/ubuntu/mediBO-runner/devcmd.sh rpc legal_get_page '{"p_slug":"about"}'`
   — mediBO is B2B pharma; prices are trade/PTR, NOT MRP; roles are customer/
   supplier/delivery/worker/MR/admin. Recommendations must match this model.
3. Prior art: `/home/ubuntu/mediBO-runner/devcmd.sh rpc dev_cmd_list '{"p_limit":40}'` for how similar
   features were built recently.

THEN produce questions = ONLY real, still-undecided decisions after that
reading. Rules by mode:
  - auto  : ignore the count, ask ONLY genuine gaps (0..9), biggest decision
            first. If nothing is undecided, return an EMPTY array.
  - command: up to 5 whole-spec questions, impact-ranked.
  - point : split the spec into numbered points; set point_idx (0-based) and
            point_text on each; up to 5 questions PER point.
Also detect: contradictions (kind="contradiction", make it idx 0), hidden
decisions Om didn't mention (empty states, errors, permissions, edge cases →
kind="hidden"), missing prerequisites (kind="prereq"). If the request is really
several features, put the EXTRA ones in emit_multi as [{"spec":"..."}] so submit
can split them; mention that in the receipt.

Each question object MUST have exactly these keys:
  idx (int, 0-based, unique), point_idx (int, 0 if not point mode),
  point_text (string, "" if not point mode), q (plain-language question),
  reason (one line: why it matters), kind (gap|hidden|contradiction|prereq|scope),
  recommended (your best answer), recommended_source (where it came from, e.g.
  "schema: coupons table absent", "about: PTR pricing", "your saved choice"),
  alt (one alternative answer), confidence (high|med|low),
  cost_hint (e.g. "~2 min" vs "+1 day" when options differ materially, else ""),
  example_urls (array of image URLs, usually []), topic (short normalized slug).

receipt = 2-4 plain sentences of what you FOUND (schema state, business rules
applied, assumptions). It becomes the assumption receipt on the built command.

WHEN DONE, write your result with the guarded RPC (service key is inside the
wrapper — you do NOT handle keys). Build the JSON and run EXACTLY:

  /home/ubuntu/mediBO-runner/devcmd.sh rpc draft_set_questions "$(jq -nc --argjson q 'QUESTIONS_JSON_ARRAY' --argjson m 'EMIT_MULTI_JSON_ARRAY' --arg r 'RECEIPT_TEXT' '{p_id:20,p_questions:$q,p_emit_multi:$m,p_receipt:$r}')"

(Replace the three placeholders. emit_multi is [] when not splitting.) Verify it
returned {"ok":true}. If you truly cannot proceed, run instead:
  /home/ubuntu/mediBO-runner/devcmd.sh rpc draft_fail '{"p_id":20,"p_error":"<short reason>"}'
Do not deploy anything, do not touch Flutter, do not claim dev commands. This is
analysis only. Keep it to ONE focused pass.
