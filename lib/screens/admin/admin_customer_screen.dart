// ONE shared implementation for web + Android. File pickers, signed-image
// display and geolocation now sit behind platform wrappers (file_pick_io /
// native_signed_image / geo_position), so the same console runs on both.
export 'admin_customer_screen_web.dart';
