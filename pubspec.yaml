name: pharma_b2b
description: "mediBO - B2B Pharmacy Ordering"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.3.1+14

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.4
  flutter_svg: ^2.3.0
  file_picker: ^11.0.2
  image_picker: ^1.1.2
  csv: ^8.0.0
  syncfusion_flutter_pdf: ^33.2.7
  archive: ^3.6.1
  xml: ^6.5.0
  http: ^1.2.2
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.6
  intl: ^0.19.0
  mobile_scanner: ^5.2.3
  qr_flutter: ^4.1.0
  record: ^5.1.2
  # Realtime Android voice counting: stream raw PCM (record's stream API) to
  # Google Speech-to-Text v2 over gRPC. google_speech bundles the generated v2
  # protobuf/gRPC stubs; grpc is imported directly by speech_native_io.dart.
  # Both are reached ONLY behind an `if (dart.library.io)` conditional import, so
  # the web build never compiles them.
  grpc: ^3.2.4
  google_speech: ^5.3.0
  audioplayers: ^6.0.0
  image: ^4.0.0
  zxing2: ^0.2.3
  printing: ^5.13.0
  pdf: ^3.11.0
  google_maps_flutter: ^2.17.1
  google_maps_flutter_web: ^0.6.2+3
  flutter_polyline_points: ^2.0.0
  web: ^1.0.0
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  cached_network_image: ^3.4.1
  google_sign_in: ^7.2.0
  # Native (Android) file save/share for the "download" affordances that are a
  # browser anchor-download on web. Reached ONLY through download_bytes_stub.dart.
  share_plus: ^10.1.4
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# record 5.2.1 pins the desktop impl record_linux to <1.0.0, whose only in-range
# build (0.7.2) predates record_platform_interface 1.6.0 and no longer matches
# its hasPermission signature. Flutter registers record_linux in the Android
# Dart plugin registrant (guarded), so it must still COMPILE. 1.3.1 fixes the
# signature and still resolves record_platform_interface ^1.5.0 (i.e. the locked
# 1.6.0) — so record_android / record_web and the web build are untouched. Web
# uses record_web and never compiles this package.
dependency_overrides:
  record_linux: 1.3.1
  # path_provider_android 2.3.x pulls the native `jni` plugin, which forces an
  # Android NDK toolchain download (~3GB) — impossible on this build host (disk
  # ~97% full). 2.2.17 is the last release before jni and is API-identical for
  # our use (getTemporaryDirectory). Pinning it keeps the Android build NDK-free.
  path_provider_android: 2.2.17

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/whatsapp.svg
    - assets/sounds/
    - assets/images/

  # DM Sans (SIL Open Font License 1.1) — CHANGE #673. Bundled rather than
  # pulled from a CDN so the storefront types correctly offline and on a weak
  # connection, and so the first frame never flashes a fallback face.
  fonts:
    - family: DMSans
      fonts:
        - asset: assets/fonts/DMSans-400.ttf
          weight: 400
        - asset: assets/fonts/DMSans-500.ttf
          weight: 500
        - asset: assets/fonts/DMSans-600.ttf
          weight: 600
        - asset: assets/fonts/DMSans-700.ttf
          weight: 700
        - asset: assets/fonts/DMSans-800.ttf
          weight: 800
        - asset: assets/fonts/DMSans-900.ttf
          weight: 900

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
